<funcname> = add
<funcname> = div
<funcname> = sub
<funcname> = mul
<varname> = $a 
<varname> = $b
<varname> = $c 
<varname> = $d
<params> = (param <varname> i32) (param <varname> i32) 
<params> = (param <varname> f64) (param <varname> f64)
<params> = (param <varname> i64) (param <varname> i64)
<params> = (param <varname> f32) (param <varname> f32)
<result> = (result i32)
<result> = (result f64)
<instruction> = get_local <varname> get_local <varname> i32.add
<instruction> = get_local <varname> get_local <varname> f64.div
<instruction> = get_local <varname> get_local <varname> f32.sub
<instruction> = get_local <varname> get_local <varname> i64.mul
<instructions> = <instruction>
<func> = (func $<funcname> <params> <result> <instructions>)
<funcs> = <func>
<module> = (module <funcs> (export "<funcname>" (func $<funcname>)))

!begin lines
<module>;
!end lines

